Test server file

       >>> from server import app, make_nodes_and_paths
       >>> client = app.test_client()

       >>> r = client.get("/data.json")

Check helper function
       >>> data = make_nodes_and_paths("ideas.csv")
       >>> assert {'adviser':'library','name':'jquery'} in data[0]

Check that nodes were built:
       >>> assert b'"nodes":' in r.data
       >>> assert b'"adviser":"ahmad","name":"Ritu"' in r.data

Check for paths
       >>> assert b'"paths":[' in r.data
       >>> assert b'"source":12,"target":7' in r.data

Check server paths for basic and advanced
       >>> r = client.get("/")
       >>> assert b"d3.json('/data.json', function(data){" in r.data

       >>> r = client.get("/advanced")
       >>> assert b'd3.json("/data.json", makeForceGraph);' in r.data
